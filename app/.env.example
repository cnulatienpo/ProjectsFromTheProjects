VITE_APP_NAME=Projects From The Projects
VITE_DEV_PORT=5173
VITE_DEBUG=false
VITE_API_BASE=/api
# Do NOT put secrets here.
VITE_MAINTENANCE_MSG=
VITE_APP_VERSION=0.1.0

import { log, wireGlobalLogging } from '@/lib/logger.ts'
wireGlobalLogging(import.meta.env.VITE_APP_VERSION || 'dev')

componentDidCatch(e: unknown) {
  log.error('render_crash', { error: String(e), remote: true })
}
