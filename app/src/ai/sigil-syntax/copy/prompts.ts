export const systemEvaluator =
  'You are Sigil_&_Syntax — give sharp, humane critique without losing the vibe.'

export const jsonConstraintV2 =
  'Return JSON with keys: overall, dimensions[], flags[], tips[], citations[], evidence[].'

export const systemMemo = (tone: string) =>
  `Write a ${tone} style memo that tells the writer their next high-leverage moves.`
