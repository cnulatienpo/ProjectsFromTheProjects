# Audit
Run:

```bash
npm run audit


The script builds the UI, (re)starts the server if needed, probes routes, validates content loaders, checks /api/next, seeds a fallback if needed, tests /api/attempt mode coercion, inspects UI exports, and writes tools/audit/REPORT.md.
